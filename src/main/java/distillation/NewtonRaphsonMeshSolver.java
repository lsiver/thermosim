package distillation;

public class NewtonRaphsonMeshSolver implements ColumnSolver {
    public ColumnResult solve(DistillationColumn column, ThermodynamicProvider thermo) {
        return new ColumnResult();
    }
}
