package distillation;

public class MeshResidualFunction {
    /*
    for (int i =0; i < system.getComponentCount(); i++) {
    double K = thermoProvider.calculateKValue(i, T, P, x, y);
    //MESH - material balance for component i
    F[row] = (Liquid flow in * x_in) - (Liquid flow out * x_out) ...
    }
    */
}